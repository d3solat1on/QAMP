<Window x:Class="QAMP.Windows.ShowInfoPlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QAMP.Windows"
        xmlns:converters="clr-namespace:QAMP.Converters"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        Width="1000"
        Height="800"
        Title="Подробная информация"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="ScrollViewer">
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Resources>
    <Border Background="#1E1E1E"
            CornerRadius="5"
            BorderBrush="#333"
            BorderThickness="1">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0"
                  Margin="20,15,20,0"
                  Height="40">
                <TextBlock Text="Подробная информация о плейлисте"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
                <Button x:Name="CloseButton"
                        Content="✕"
                        Click="CloseButton_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{StaticResource CloseButtonStyle}"
                        Background="{DynamicResource AccentBrush}"
                        Width="30"
                        Height="30"/>
            </Grid>
            <ScrollViewer Grid.Row="1" Margin="20">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="340"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Width="320"
                                Height="320"
                                CornerRadius="10"
                                Background="#2E2E2D"
                                Padding="8"
                                ClipToBounds="True">
                            <Grid>
                                <Image Source="{Binding CoverImage, Converter={StaticResource ByteArrayToImageConverter}}"
                                       Stretch="UniformToFill"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CoverImage}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CoverImage.Length}" Value="0">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="Обложка отсутствует"
                                           Foreground="#888"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"
                                           Visibility="Collapsed">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CoverImage}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CoverImage.Length}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="25,0,0,0">
                            <TextBlock Text="{Binding Name}"
                                       FontSize="28"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Description}"
                                       FontSize="14"
                                       Foreground="#AAA"
                                       Margin="0,12,0,20"
                                       TextWrapping="Wrap"
                                       MaxHeight="120"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="ID:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Id}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Треков:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TrackCountDisplay}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Длительность:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TotalDurationDisplay}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Создано:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CreatedDateDisplay}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Закреплен:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding PinnedDisplay}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Системный:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SystemPlaylistDisplay}" Foreground="White" Margin="0,4,0,4"/>

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Сортировка:" Foreground="#888" Margin="0,4,12,4"/>
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SortTypeDisplay}" Foreground="White" Margin="0,4,0,4"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Треки в плейлисте"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,24,0,12"/>

                    <ListView ItemsSource="{Binding Tracks}"
                              Background="#2E2E2E"
                              BorderThickness="0"
                              Foreground="White"
                              MinHeight="220">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="225"/>
                                        <ColumnDefinition Width="225"/>
                                        <ColumnDefinition Width="225"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Executor}"
                                               Grid.Column="1"
                                               FontSize="12"
                                               Foreground="#AAA"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Duration}"
                                               Grid.Column="2"
                                               FontSize="12"
                                               Foreground="#AAA"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>