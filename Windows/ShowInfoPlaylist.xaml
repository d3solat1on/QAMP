<Window x:Class="QAMP.Windows.ShowInfoPlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QAMP.Windows"
        xmlns:converters="clr-namespace:QAMP.Converters"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        Width="1000"
        Height="800"
        Title="Подробная информация"
        WindowStartupLocation="CenterOwner">
        <Window.Resources>
                <converters:LargeTrackImageConverter x:Key="LargeTrackImageConverter"/>
                <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                <Style TargetType="ScrollViewer">
                        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
                                Value="Disabled"/>
                        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
                                Value="Auto"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                </Style>
        </Window.Resources>
        <Border Background="{DynamicResource CentralPanelCardBackground}"
                CornerRadius="5"
                BorderBrush="{DynamicResource AccentBrush}"
                BorderThickness="1">
                <Grid Margin="0">
                        <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0"
                              Margin="20,15,20,0"
                              Height="40">
                                <TextBlock Text="Подробная информация о плейлисте"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                <Button x:Name="CloseButton"
                                        Content="✕"
                                        Click="CloseButton_Click"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Style="{StaticResource CloseButtonStyle}"
                                        Background="{DynamicResource AccentBrush}"
                                        Width="30"
                                        Height="30">
                                        <Button.Resources>
                                                <Style TargetType="Border">
                                                        <Setter Property="CornerRadius"
                                                                Value="2"/>
                                                </Style>
                                        </Button.Resources>
                                </Button>
                        </Grid>
                        <Grid Grid.Row="1"
                              Margin="20">
                                <Grid>
                                        <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="340"/>
                                                <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0"
                                                Width="320"
                                                Height="320"
                                                CornerRadius="10"
                                                Background="{DynamicResource CentralPanelCardBackground}"
                                                Padding="8"
                                                ClipToBounds="True">
                                                <Grid>
                                                        <Image Source="{Binding CoverImage, Converter={StaticResource LargeTrackImageConverter}}"
                                                               Stretch="UniformToFill"
                                                               Margin="2">
                                                                <Image.Style>
                                                                        <Style TargetType="Image">
                                                                                <Setter Property="Visibility"
                                                                                        Value="Visible"/>
                                                                                <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding CoverImage}"
                                                                                                     Value="{x:Null}">
                                                                                                <Setter Property="Visibility"
                                                                                                        Value="Collapsed"/>
                                                                                        </DataTrigger>
                                                                                        <DataTrigger Binding="{Binding CoverImage.Length}"
                                                                                                     Value="0">
                                                                                                <Setter Property="Visibility"
                                                                                                        Value="Collapsed"/>
                                                                                        </DataTrigger>
                                                                                </Style.Triggers>
                                                                        </Style>
                                                                </Image.Style>
                                                        </Image>

                                                        <Grid x:Name="PlaylistPlaceholderContainer">
                                                                <Grid.Style>
                                                                        <Style TargetType="Grid">
                                                                                <Setter Property="Visibility"
                                                                                        Value="Collapsed"/>
                                                                                <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding CoverImage}"
                                                                                                     Value="{x:Null}">
                                                                                                <Setter Property="Visibility"
                                                                                                        Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                        <DataTrigger Binding="{Binding CoverImage.Length}"
                                                                                                     Value="0">
                                                                                                <Setter Property="Visibility"
                                                                                                        Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                </Style.Triggers>
                                                                        </Style>
                                                                </Grid.Style>

                                                                <Path Fill="{DynamicResource AccentBrush}"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      Stretch="Uniform">
                                                                        <Path.Style>
                                                                                <Style TargetType="Path">
                                                                                        <Setter Property="Data"
                                                                                                Value="{StaticResource PlugForTheRightSideGeometry}"/>
                                                                                        <Style.Triggers>
                                                                                                <DataTrigger Binding="{Binding Id}"
                                                                                                             Value="1">
                                                                                                        <Setter Property="Data"
                                                                                                                Value="{StaticResource FavoriteCoverGeometry}"/>
                                                                                                </DataTrigger>
                                                                                        </Style.Triggers>
                                                                                </Style>
                                                                        </Path.Style>
                                                                </Path>
                                                        </Grid>

                                                </Grid>
                                        </Border>
                                        <StackPanel Grid.Column="1"
                                                    Margin="25,0,0,0">
                                                <TextBlock x:Name="NameTextBlock"
                                                           Text="{Binding Name}"
                                                           FontSize="28"
                                                           FontWeight="Bold"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap"/>
                                                <TextBlock x:Name="DescriptionTextBlock"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                           Margin="0,12,0,20"
                                                           TextWrapping="Wrap"
                                                           MaxHeight="120"/>

                                                <Grid>
                                                        <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <TextBlock x:Name="IdTextBlock"
                                                                   Grid.Row="0"
                                                                   Grid.Column="0"
                                                                   Text="ID:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="IdValueTextBlock"
                                                                   Grid.Row="0"
                                                                   Grid.Column="1"
                                                                   Text="{Binding Id}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="TracksCountTextBlock"
                                                                   Grid.Row="1"
                                                                   Grid.Column="0"
                                                                   Text="Треков:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="TracksCountValueTextBlock"
                                                                   Grid.Row="1"
                                                                   Grid.Column="1"
                                                                   Text="{Binding TrackCountDisplay}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="TotalDurationTextBlock"
                                                                   Grid.Row="2"
                                                                   Grid.Column="0"
                                                                   Text="Длительность:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="TotalDurationValueTextBlock"
                                                                   Grid.Row="2"
                                                                   Grid.Column="1"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="CreatedDateTextBlock"
                                                                   Grid.Row="3"
                                                                   Grid.Column="0"
                                                                   Text="Создано:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="CreatedDateValueTextBlock"
                                                                   Grid.Row="3"
                                                                   Grid.Column="1"
                                                                   Text="{Binding CreatedDateDisplay}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="PinnedTextBlock"
                                                                   Grid.Row="4"
                                                                   Grid.Column="0"
                                                                   Text="Закреплен:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="PinnedValueTextBlock"
                                                                   Grid.Row="4"
                                                                   Grid.Column="1"
                                                                   Text="{Binding PinnedDisplay}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="SystemPlaylistTextBlock"
                                                                   Grid.Row="5"
                                                                   Grid.Column="0"
                                                                   Text="Системный:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="SystemPlaylistValueTextBlock"
                                                                   Grid.Row="5"
                                                                   Grid.Column="1"
                                                                   Text="{Binding SystemPlaylistDisplay}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>

                                                        <TextBlock x:Name="SortTypeTextBlock"
                                                                   Grid.Row="6"
                                                                   Grid.Column="0"
                                                                   Text="Сортировка:"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,12,4"/>
                                                        <TextBlock x:Name="SortTypeValueTextBlock"
                                                                   Grid.Row="6"
                                                                   Grid.Column="1"
                                                                   Text="{Binding SortTypeDisplay}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   Margin="0,4,0,4"/>
                                                </Grid>
                                        </StackPanel>
                                </Grid>
                        </Grid>
                        <Grid Grid.Row="2"
                              Margin="25,0,0,0">
                                <DataGrid x:Name="TracksInPlaylist"
                                          ItemsSource="{Binding Tracks}"
                                          AlternationCount="2"
                                          FontSize="12">
                                        <DataGrid.Resources>
                                                <Style TargetType="{x:Type DataGridRowHeader}">
                                                        <Setter Property="Background"
                                                                Value="Transparent"/>
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                        <Setter Property="Width"
                                                                Value="0"/>
                                                </Style>
                                                <Style TargetType="DataGridColumnHeader">
                                                        <Setter Property="Background"
                                                                Value="{DynamicResource DataGridHeaderBackground}"/>
                                                        <Setter Property="Foreground"
                                                                Value="{DynamicResource DataGridHeaderForeground}"/>
                                                        <Setter Property="FontWeight"
                                                                Value="Bold"/>
                                                        <Setter Property="FontSize"
                                                                Value="12"/>
                                                        <Setter Property="Height"
                                                                Value="40"/>
                                                        <Setter Property="BorderBrush"
                                                                Value="{DynamicResource GridHeaderSeperatorBrush}"/>
                                                        <Setter Property="BorderThickness"
                                                                Value="0,0,1,1"/>
                                                        <Setter Property="HorizontalContentAlignment"
                                                                Value="Left"/>
                                                        <Setter Property="Padding"
                                                                Value="10,0,0,0"/>
                                                </Style>

                                                <Style TargetType="DataGridCell">
                                                        <Setter Property="Background"
                                                                Value="{DynamicResource DataGridCellBackground}"/>
                                                        <Setter Property="Foreground"
                                                                Value="{DynamicResource DataGridCellForeground}"/>
                                                        <Setter Property="BorderBrush"
                                                                Value="Transparent"/>
                                                        <Setter Property="VerticalAlignment"
                                                                Value="Center"/>
                                                        <Setter Property="Padding"
                                                                Value="10,5,0,5"/>
                                                        <Setter Property="FontSize"
                                                                Value="12"/>
                                                        <Style.Triggers>
                                                                <Trigger Property="IsSelected"
                                                                         Value="True">
                                                                        <Setter Property="Background"
                                                                                Value="{DynamicResource DataGridRowSelectedBackground}"/>
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource DataGridCellSelectedForeground}"/>
                                                                        <Setter Property="BorderBrush"
                                                                                Value="{DynamicResource DataGridSelectionBorderBrush}"/>
                                                                </Trigger>
                                                        </Style.Triggers>
                                                </Style>

                                                <Style TargetType="DataGridRow">
                                                        <Setter Property="Background"
                                                                Value="{DynamicResource DataGridRowBackground}"/>
                                                        <Setter Property="MinHeight"
                                                                Value="35"/>
                                                        <Style.Triggers>
                                                                <Trigger Property="AlternationIndex"
                                                                         Value="1">
                                                                        <Setter Property="Background"
                                                                                Value="{DynamicResource DataGridRowAltBackground}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsSelected"
                                                                         Value="True">
                                                                        <Setter Property="Background"
                                                                                Value="{DynamicResource DataGridRowSelectedBackground}"/>
                                                                </Trigger>
                                                        </Style.Triggers>
                                                </Style>
                                        </DataGrid.Resources>

                                        <DataGrid.Columns>
                                                <DataGridTextColumn Header="Title"
                                                                    Binding="{Binding Name}"
                                                                    Width="2*">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="TextBlock.Foreground"
                                                                                Value="{DynamicResource CentralPanelSecondaryForeground}"/>
                                                                        <Setter Property="TextBlock.Margin"
                                                                                Value="10,0,0,0"/>
                                                                        <Setter Property="TextBlock.VerticalAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="Artist"
                                                                    Binding="{Binding Executor}"
                                                                    Width="2*">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="TextBlock.Foreground"
                                                                                Value="{DynamicResource CentralPanelSecondaryForeground}"/>
                                                                        <Setter Property="TextBlock.Margin"
                                                                                Value="10,0,0,0"/>
                                                                        <Setter Property="TextBlock.VerticalAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="Duration"
                                                                    Binding="{Binding Duration}"
                                                                    Width="2*">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="TextBlock.Foreground"
                                                                                Value="{DynamicResource CentralPanelSecondaryForeground}"/>
                                                                        <Setter Property="TextBlock.Margin"
                                                                                Value="10,0,0,0"/>
                                                                        <Setter Property="TextBlock.VerticalAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="ID"
                                                                    Binding="{Binding Id}"
                                                                    Width="2*">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="TextBlock.Foreground"
                                                                                Value="{DynamicResource CentralPanelSecondaryForeground}"/>
                                                                        <Setter Property="TextBlock.Margin"
                                                                                Value="10,0,0,0"/>
                                                                        <Setter Property="TextBlock.VerticalAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="Date Added"
                                                                    Binding="{Binding AddedDateDisplay}"
                                                                    Width="2*">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="TextBlock.Foreground"
                                                                                Value="{DynamicResource CentralPanelSecondaryForeground}"/>
                                                                        <Setter Property="TextBlock.Margin"
                                                                                Value="10,0,0,0"/>
                                                                        <Setter Property="TextBlock.VerticalAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                        </DataGrid.Columns>
                                </DataGrid>
                        </Grid>
                </Grid>
        </Border>
</Window>